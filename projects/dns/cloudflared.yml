cloudflared:
  container_name: cloudflared
  image: visibilityspots/cloudflared:latest
  restart: unless-stopped
  environment:
    - TZ=${TZ}
    - DNS1='1.1.1.1'
    - DNS2='1.0.0.1'
    - PORT=5054
  network_mode: host
  ports:
    - 5054:5054
