redis:
  image: redis:6.2.3-alpine
  container_name: redis
  ports:
    - 6379:6379
  volumes:
    - /etc/localtime:/etc/localtime:ro
    - ${VOLUMES}/redis:/data
  stop_grace_period: 30s
  stop_signal: SIGQUIT
