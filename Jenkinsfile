pipeline {
    agent any
    environment {
        SOME_KEY    = credentials('docker-some-key')
        OTHER_KEY   = credentials('docker-other-key')
    }
    stages {
        stage('Example stage 1') {
            steps {
                // 
            }
        }
        stage('Example stage 2') {
            steps {
                // 
            }
        }
    }
}